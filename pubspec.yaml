name: imsnsit
description: "NSUT Attendance"
publish_to: "none"

version: 1.0.5+3

environment:
  sdk: ">=3.2.3 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  cookie_store: ^0.3.5
  html:
  provider: ^6.1.1
  cached_network_image: ^3.3.1
  shared_preferences: ^2.2.2
  google_fonts: ^6.1.0
  flutter_cache_manager: ^3.3.1
  catppuccin_flutter: ^1.0.0
  go_router: ^13.0.1
  url_launcher: ^6.2.2
  loader_overlay: ^4.0.0
  flutter_native_splash: ^2.3.9
  flutter_animate: ^4.4.0
  path_provider:
  intl:

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0
  auto_route_generator:

flutter:
  uses-material-design: true

  assets:
    - assets/nsut.png
    - assets/user.png
    - assets/github.png
